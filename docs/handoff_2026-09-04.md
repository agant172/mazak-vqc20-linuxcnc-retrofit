# Session handoff — 2026-09-04

> Written per the new CLAUDE.md session-hygiene rule: what was done, what was
> found, what's unverified, and where the details live.

## What this session did (continuing from 2026-09-01/02/03)

Three sequential Fable-audited passes over the retrofit, each finished with
owner-approved fixes applied and pushed:

1. **Ladder + signal-map audit** (2026-09-02) — 10 parallel agents rung-by-rung
   corrected every ladder transcription against `YM2V39L.pdf` and reconciled
   the pin authority. Report: `docs/ladder_signal_audit_2026-09-02.md`.
2. **Wiring-doc audit** (2026-09-03) — 8 parallel agents (+ a 3rd independent
   BBIA-1 read) verified `wiring/*` against `41434WB.pdf`, found a systematic
   pinout misread layer, corrected ~100 cells at the root, landed 5
   owner-approved HAL-bound rebindings. Report: `docs/wiring_audit_2026-09-03.md`.
3. **LinuxCNC config audit** (2026-09-03/04) — 6 parallel agents audited every
   `.hal`/`.comp`/`.ngc`/`.ini` file + the test harness against the corrected
   ladder. Found and fixed a **load-blocking HAL bug** (pin double-linked to
   two signals — LinuxCNC could not have started). Report:
   `docs/linuxcnc_config_audit_2026-09-03.md` — carries a 16-item owner
   decision queue, now partially applied (see below).

Then, from two owner-supplied cabinet photos (IMG_0091/IMG_0097) plus owner
firsthand knowledge (detent-ball hardware he'd personally replaced):

4. **Bench items 20 and 40 closed from photos**: magazine confirmed **30 pots**
   (not the assumed 20), fork-style pot retention confirmed (spring + detent
   ball each side, not a socket).
5. **Owner said "yes go"** — both gates for the crash-class fixes cleared.
   Applied fixes 1–4 from the decision queue: resequenced `toolchange.ngc` to
   the corrected horizontal fork-entry exchange (was a vertical-plunge
   geometry that would have crashed on cycles E and F), fixed the ATC
   magazine-rotate deadlock, fixed a self-faulting AL75/AL76 timing bug, added
   the missing `G20` (metric-unit safety bug).
6. **One more Fable print-read** (TSINTL coil rung + 30TS*2 + pot-encoding +
   D16) closed the last open questions from the resequencing and caught a
   **second, independent defect**: the transcribed BCD pot-encoding was wrong
   for this machine's magazine size. 24TS/30TS magazines use straight binary
   (weights 1/2/4/8/16), not BCD (1/2/4/8/10) — fixed in the comp, the test
   suite, and the transcription.

**Every fix in this session is compile-checked and/or harness-tested on the
LinuxCNC box before commit.** Current state: 12 scenarios, 407 checks, 0
failed. All work pushed to `origin/main` through commit `861a25e`.

## What's still open (owner decisions, not applied)

`docs/linuxcnc_config_audit_2026-09-03.md` — items 5–16 of the behavior
queue. Highest-value remaining:

- **Item 5 (orient gear-solenoid topology)** — the comp implements the
  pre-audit reading of rungs 2907/2910. As coded, both gear-shift solenoids
  go dead mid-shift with the spindle still turning (no OEM-style
  outgoing-gear hold), and `CTL`/`OUT5` reads FALSE during a low-gear orient
  that needed no shift — which would mis-orient the FR-SX exactly when the
  ATC needs the latch. This is the single highest-value fix left undone.
- **Item 10 (return-pot staging)** — nothing stages the old tool's pot before
  D/E unclamp; the old tool drops wherever the magazine happens to sit. Needs
  a design decision (fixed-pocket vs `RANDOM_TOOLCHANGER`) before it's
  fixable in code.
- **Item 12 (spindle top speed)** — INI says 4000 RPM; the captured M-2
  parameter (GH4) says 3488. Also flags a harder problem: one `OUTPUT_SCALE`
  can't correctly command both gear ranges (low-gear S400 would command
  ~1 V) — needs a gear-aware analog scale design, not a one-line fix.
- Items 6, 7, 9, 11, 13–16 are smaller — full detail and physical
  consequences are in the report.

## Bench-check list

The phone artifact (same URL each update — see the shop-projects/audit memory
for the link, or ask "show me the bench checklist") is at 45 items, of which
**#20 and #40 are answered** (mark them done). **#41 is now answered by the
print read** (mark it done — TSINTL is print-resolved, no bench trip needed).
**#42 changed shape**: it's no longer "confirm pot 20 is all-off," it's now
"read pots 16/20/26/30 to confirm the weight-16 binary encoding" — the phone
page has the corrected wording.

Nothing else in the checklist changed this session. Items 1–19, 21–39, 43–45
are still open exactly as last described.

## Does the next task need this session, or would a fresh session do as well?

**A fresh session pointed at this doc would do equally well for most
next steps** — the three audit reports plus this handoff carry every fact a
new session needs (file locations, what's verified, what's queued, current
commit). Specifically:

- **Bench work** (any of the 45 checklist items): fresh session is fine —
  it's mechanical recording of measurements against a known-current checklist.
- **Applying more decision-queue items** (e.g. "go ahead on item 5, the
  gear-solenoid fix"): fresh session is fine — the queue report has full
  before/after logic and citations; no session memory is load-bearing.
- **A brand-new audit domain** (e.g. auditing the resolver commissioning
  docs, or the E-stop hardwired chain): fresh session, using the same
  `notes/fable_ladder_audit_prompt.md` playbook — no continuity needed.

**Where this session's context would still help**: if the very next thing is
a quick follow-up question about *why* a specific edit was made (the
reasoning trail across the three audits, the back-and-forth on the 30-pot
discovery), this session has that texture in a way a fresh read of the
commits doesn't fully capture. But that's a convenience, not a requirement —
the commit messages themselves are written to stand alone.

**Recommendation: safe to start fresh for the next piece of work.** This repo
is now in unusually good shape for a clean handoff — every fact worth keeping
already made it into a file.

## Addendum 2026-09-04 (later session) — items 14 and 16a decided and applied

Owner decisions taken in a separate review session and applied here; a
parallel session owns items 9b and 10 (see its own commits).

- **Item 14 BACKLASH — hold at 0.0.** Comments on all three `BACKLASH` lines
  in `mazak_vqc_20_40.ini` record the captured M-2 values as reference only;
  measure with an indicator after loop tuning.
- **Item 16a spindle-at-speed — RESOLVED, one wire, not "derive in HAL".**
  The FR-SX has a discrete up-to-speed output the machine drawing never used:
  maintenance manual BCN-21735-S5 p12, **CON3 pin 15 `USO`**, COM pin 20,
  transistor ON within ±15 % of commanded speed, LED7 on the SX-CPU lights
  with it. `SPINDLE_AT_SPEED` CSV row DEFERRED → COMMISSIONING_PENDING with
  dest `FR-SX CON3-15`; IN13 comment rewritten in `field_7i84u.hal`; Finding 7
  added to `docs/frsx_maintenance_manual_notes.md`; **bench item 46** is the
  polarity measurement. Interim on the bench: jumper IN13→VFIELD (the
  validator forbids a HAL `sets` on this signal, deliberately).
- Also learned, handed to the 9b/10 session: `#<_selected_tool>` is 0 for
  "no T ever" AND explicit T0 (rs274-verified + source), so a `LT 0` guard
  never fires; iocontrol's `tool-prepare` pulses for every T including T0, so
  a HAL flipflop latch is the clean "T was issued" flag. And cycle F never
  indexes the magazine at all — the return-stage leg should become a
  pre-entry index for all three cycles.
- Validators: `validate_authority.py` 0 errors / 1 pre-existing warning
  (COOLANT_ON wire number); `validate_control_logic.py` clean.

Does the next task need this session? No — everything above is on disk.

### Addendum 2 — cycle F pre-entry index + rs274 syntax check

- **Cycle F now indexes** to the commanded pot in the same pre-entry window
  the D/E return staging uses (Z to `z_clear`, P3 index-request, wait MSTP).
  Before this, load-only changes never indexed the magazine at all.
- **`toolchange.ngc` could not have loaded.** Running it through `rs274 -i
  mazak_vqc_20_40.ini -g` on the LinuxCNC box raised `Nested comment found`
  at header line 12 — 85 comment lines used parentheses inside parentheses,
  which the RS274NGC reader rejects. All inner parens are now square
  brackets. New `tests/ngc/rs274_check.sh` parses every `remap/*.ngc` this
  way; run it on the box alongside `tests/hal/run_tests.py`. It does not
  exercise M66 waits — the sim end-to-end test is still the gap.

### Addendum 3 — 2026-09-05: items 11a, 13D, 16c decided and applied

Owner, in one line: "bless 11a and 13D as-is, apply them. 16c warning only."

- **11a (AL45 consequence) — BLESSED.** Comment-only: the AL45 block in
  `mazak_orient.comp` now says the AL45-holds-ORCM1 / AL46-drops-arm split is
  owner-blessed and matches the ladder, so nobody re-opens it.
- **13D (HOME_SEQUENCE order Z, X, Y) — BLESSED.** INI comment updated; only
  bench item 7 (Y switch side) can re-open it.
- **16c (orphan safety inputs) — WARNING-ONLY, all three.** New pyvcp panel
  `linuxcnc/panel_warnings.xml` (`[DISPLAY] PYVCP`), three LEDs netted in
  `postgui.hal` from `thermal-alarm` / `door-interlock` / `lube-ok`. No
  interlock consequence. Recorded as a **DELIBERATE DEVIATION** from the OEM
  PLC alarms AL57 / AL55 / AL56 (which halted the machine) in the HAL comments,
  the authority CSV rows, and the config-audit report. Hardwired door lock and
  motor thermal protection stay OEM. **Bench item 47** added (IN8 thermal-chain
  polarity); door and lube polarity were already bench items 8 and 11.
- Verified on the LinuxCNC box: components reinstalled, harness 12 scenarios /
  410 checks / 0 failed, `rs274_check.sh` ok on both NGC files, panel XML
  load-tested under `xvfb-run halrun` with all three pyvcp pins bound.
  `validate_authority.py` 0 errors / 1 pre-existing warning;
  `validate_control_logic.py` clean; label CSVs, wire sheets and
  `io-dashboard/data.js` regenerated (data.js had been stale since 2026-08-23).

Still open after this: 11b (bench: X005 vs X05B identity), 11d (TCME
architecture call), 12b (bench: volts-per-RPM both gears), 13C (bench item 7),
16b (bench: amp ALM contact), orient B3 (drive_arm in the gear-coil equations).
